[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python main.py"
# كل 5 دقائق: يفحص لو في trigger يدوي أو وقت الـ run اليومي (06:00 UTC)
# لو مفيش، بيخرج في ثواني بدون تكلفة
cronSchedule = "*/5 * * * *"
restartPolicyType = "NEVER"
